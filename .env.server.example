# Audit Server Configuration

# Server listen address
SERVER_LISTEN=:8080

# PostgreSQL connection string
# Format: host=localhost port=5432 user=postgres password=YOUR_PASSWORD dbname=audit sslmode=disable
POSTGRES_DSN=host=localhost port=5432 user=postgres password=200603 dbname=audit sslmode=disable
